# Prompt: extend an existing demo

Extend an existing demo while preserving its lightweight structure.

Requirements:
- Keep changes small and explain why each change is needed.
- Update `README.md` and `metadata.json` if scope changes.
- Add only the minimum infra/code required to validate the next hypothesis.
