# Prompt: create a new feasibility demo

Create a new demo under `demos/<demo-name>/` using `demo-template/` as the base.

Requirements:
- Keep it minimal and runnable.
- Focus on one customer problem and one testable hypothesis.
- Populate `metadata.json` fully.
- Add setup steps in plain language.
- Mark optional production controls as optional.
